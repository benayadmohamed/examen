spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://localhost:8888
#    gateway:
#      routes:
#        - id: security-service-route-id
#          uri: lb://security-service
#          predicates:
#            - "Path=/user/**"
#          filters:
#            - RewritePath=/user(?<segment>/?.*), $\{segment}
